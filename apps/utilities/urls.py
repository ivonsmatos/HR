"""Utilities app URLs."""
from django.urls import path

app_name = "utilities"

urlpatterns = [
    # Placeholder for future Utilities endpoints
    # path('tickets/', TicketListView.as_view(), name='ticket-list'),
    # path('assets/', AssetListView.as_view(), name='asset-list'),
]
