"""Work app URLs."""
from django.urls import path

app_name = "work"

urlpatterns = [
    # Placeholder for future Work endpoints
    # path('projects/', ProjectListView.as_view(), name='project-list'),
    # path('tasks/', TaskListView.as_view(), name='task-list'),
]
