"""HRM app URLs."""
from django.urls import path

app_name = "hrm"

urlpatterns = [
    # Placeholder for future HRM endpoints
    # path('employees/', EmployeeListView.as_view(), name='employee-list'),
    # path('leaves/', LeaveListView.as_view(), name='leave-list'),
]
