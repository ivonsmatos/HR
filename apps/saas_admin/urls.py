"""SaaS Admin app URLs."""
from django.urls import path

app_name = "saas_admin"

urlpatterns = [
    # Placeholder for future SaaS Admin endpoints
    # path('subscriptions/', SubscriptionListView.as_view(), name='subscription-list'),
    # path('plans/', SubscriptionPlanListView.as_view(), name='plan-list'),
]
