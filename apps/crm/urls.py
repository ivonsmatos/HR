"""CRM app URLs."""
from django.urls import path

app_name = "crm"

urlpatterns = [
    # Placeholder for future CRM endpoints
    # path('clients/', ClientListView.as_view(), name='client-list'),
    # path('leads/', LeadListView.as_view(), name='lead-list'),
]
