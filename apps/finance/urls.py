"""Finance app URLs."""
from django.urls import path

app_name = "finance"

urlpatterns = [
    # Placeholder for future Finance endpoints
    # path('invoices/', InvoiceListView.as_view(), name='invoice-list'),
    # path('payments/', PaymentListView.as_view(), name='payment-list'),
]
